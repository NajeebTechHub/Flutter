import "dart:io";
import 'dart:math';

void main() {

  /*int ran, usern;

  Random n = Random();
  ran = n.nextInt(100);

  print("Guess the umber generated by the system from 1 to 100");

  while(true){
    stdout.write("Enter you guessed number : ");
    usern = int.parse(stdin.readLineSync()!);

    if(usern < ran){
      print("your guess number is low, try again");
    }
    else if(usern > ran && usern <= 100){
      print("your gussed number is high, try again");
    }else if(usern > 100){
      print("invalid number, guess from 1 to 100");
    }
    else{
      print("wow congrats you guess the number");
      break;
    }
  }*/

 /* int num1, num2, user, choice,correct;
  double dev;

  String message = '''
    1:Add
    2:Sub
    3:Mul
    4:Dev
    ''';
  stdout.write("Enter your choice : ");
  choice = int.parse(stdin.readLineSync()!);

  int i = 1;
  while ( i <= 10) {


    Random random = Random();
    num1 = random.nextInt(8) + 2;
    num2 = random.nextInt(8) + 2;

  switch(choice) {
    case 1:
         correct = num1 + num2;

         stdout.write("Q$i: $num1 + $num2 = ");

         user = int.parse(stdin.readLineSync()!);

      if(user == correct)
           {
            print("Correct");
           }
      else{
           print("Incorrect");
           print("Correct answer is = $correct");
          }
      break;

    case 2:
          correct = num1 - num2;

          stdout.write("Q$i: $num1 - $num2 = ");

          user = int.parse(stdin.readLineSync()!);

       if(user == correct)
       {
         print("Correct");
        }
      else
      {
        print("Incorrect");
        print("Correct answer is = $correct");
      }
     break;
    case 3:
          correct = num1 * num2;

          stdout.write("Q$i: $num1 x $num2 = ");

          user = int.parse(stdin.readLineSync()!);

      if(user == correct)
      {
       print("Correct");
      }
     else
      {
        print("Incorrect");
        print("Correct answer is = $correct");
      }
      break;
    case 4:
            dev = num1 / num2;

            stdout.write("Q$i: $num1 / $num2 = ");

            user = int.parse(stdin.readLineSync()!);

        if(user == dev)
        {
          print("Correct");
         }
        else
         {
           print("Incorrect");
           print("Correct answer is = $dev");
          }
         break;
      }
      i++;
     }
    if(choice > 4) {
    print("Invalid number");
  }*/


}
